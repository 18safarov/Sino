body {
    font-family: Arial, sans-serif;
}

header {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 20px;
}

.slider {
    max-width: 600px;
    margin: 20px auto;
    position: relative;
    overflow: hidden;
}

.slider img {
    width: 100%;
    display: none;
}

#info {
    max-width: 600px;
    margin: 20px auto;
    text-align: center;
}
